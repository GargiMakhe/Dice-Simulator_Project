# Dice-Simulator_Project
This Python program simulates a dice roll using the random module. It asks the user if they want to roll the dice and generates a random number between 1 and 6 to display the result. The program repeats until the user chooses to stop and also handles invalid inputs.
